# Sprint Archive: v0.33.4

## 🚀 Released v0.33.4 (Phantom Video & Documentation)
- [x] **Phantom Video Fix (Phase 14)**: Clip 334 ("2.3 Investigators Mobilise") showed a blank Latest Result viewer with a phantom filename. Physical `.mp4` file was deleted from disk but `resultUrl` still referenced it. Cleaned stale DB entry; audited all clips — only clip 334 affected.
- [x] **Sideload Duplication Fix (Phase 14)**: The "← sideload to MIS" button in `LatestResultViewer.tsx` added the result to MIS slots but never cleared `resultUrl`, allowing infinite phantom duplicates. Added `onClearResult` callback wired through `BatchEditModalV3.tsx` that calls `onSave({ resultUrl: '', thumbnailPath: '', isPersisted: false })`.
- [x] **Playbook Update (Phase 15)**: Corrected Next.js version (13.5.6), added Zustand/Kling/Nano to tech stack, added DB sync protocol to Server Management, updated Pre-Deploy Checklist.
